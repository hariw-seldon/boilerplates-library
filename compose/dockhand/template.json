{
  "slug": "dockhand",
  "kind": "compose",
  "metadata": {
    "name": "Dockhand",
    "description": "Modern Docker management UI with Compose stack support, Git-based stack deployments, and optional OIDC authentication.",
    "tags": [
      "traefik",
      "postgresql",
      "sqlite"
    ],
    "icon": {
      "provider": "selfhst",
      "id": "dockhand"
    },
    "draft": false,
    "version": {
      "name": "v1.0.26",
      "source_dep_name": "docker.io/fnsys/dockhand",
      "source_dep_version": "v1.0.26"
    }
  },
  "variables": [
    {
      "title": "General",
      "name": "general",
      "items": [
        {
          "name": "service_name",
          "type": "str",
          "title": "Service Name",
          "required": false,
          "default": "dockhand",
          "config": {
            "placeholder": "dockhand"
          },
          "description": "Name of the service."
        },
        {
          "name": "restart_policy",
          "type": "enum",
          "title": "Restart Policy",
          "required": true,
          "default": "unless-stopped",
          "config": {
            "options": [
              "unless-stopped",
              "always",
              "on-failure",
              "no"
            ]
          },
          "description": "Policy used for Restart."
        },
        {
          "name": "encryption_key",
          "type": "secret",
          "title": "Encryption Key",
          "required": false,
          "description": "Base64-encoded 32-byte AES-256 key used for credential encryption.",
          "config": {
            "autogenerated": {
              "kind": "base64",
              "bytes": 32
            }
          }
        },
        {
          "name": "puid",
          "type": "int",
          "title": "PUID",
          "required": false,
          "config": {
            "placeholder": "1000"
          },
          "description": "Optional user ID for Dockhand. On Linux check with `id -u`."
        },
        {
          "name": "pgid",
          "type": "int",
          "title": "PGID",
          "required": false,
          "config": {
            "placeholder": "1000"
          },
          "description": "Optional group ID for Dockhand. On Linux check with `id -g`."
        }
      ]
    },
    {
      "title": "Ports",
      "name": "ports",
      "items": [
        {
          "name": "ports_http",
          "type": "int",
          "title": "HTTP Port",
          "required": true,
          "default": 3000,
          "needs": [
            "traefik_enabled=false"
          ],
          "config": {
            "placeholder": "3000"
          },
          "description": "Host port exposed for HTTP."
        }
      ]
    },
    {
      "title": "Traefik",
      "name": "traefik",
      "toggle": "traefik_enabled",
      "items": [
        {
          "name": "traefik_enabled",
          "type": "bool",
          "title": "Traefik",
          "required": false,
          "default": false,
          "description": "Enable Traefik integration"
        },
        {
          "name": "traefik_host",
          "type": "str",
          "title": "Host",
          "required": true,
          "config": {
            "placeholder": "dockhand"
          },
          "description": "Hostname used for Traefik."
        },
        {
          "name": "traefik_domain",
          "type": "str",
          "title": "Domain",
          "required": true,
          "config": {
            "placeholder": "home.arpa"
          },
          "description": "Domain used for Traefik."
        },
        {
          "name": "traefik_entrypoint",
          "type": "str",
          "title": "Entrypoint",
          "required": true,
          "default": "web",
          "description": "Entrypoint name.",
          "config": {
            "placeholder": "web"
          }
        },
        {
          "name": "traefik_network",
          "type": "str",
          "title": "Network",
          "required": true,
          "default": "traefik",
          "config": {
            "placeholder": "traefik"
          },
          "description": "Network used for Traefik."
        },
        {
          "name": "traefik_tls_enabled",
          "type": "bool",
          "title": "TLS",
          "required": false,
          "default": false,
          "description": "Enable Traefik TLS"
        },
        {
          "name": "traefik_tls_entrypoint",
          "type": "str",
          "title": "TLS Entrypoint",
          "required": true,
          "default": "websecure",
          "description": "Entrypoint name.",
          "config": {
            "placeholder": "websecure"
          },
          "needs": [
            "traefik_tls_enabled=true"
          ]
        },
        {
          "name": "traefik_tls_certresolver",
          "type": "str",
          "title": "Cert Resolver",
          "required": true,
          "default": "cloudflare",
          "needs": [
            "traefik_tls_enabled=true"
          ],
          "config": {
            "placeholder": "cloudflare"
          },
          "description": "Traefik TLS cert resolver."
        }
      ]
    },
    {
      "title": "Database",
      "name": "database",
      "items": [
        {
          "name": "database_type",
          "type": "enum",
          "title": "Database Type",
          "required": false,
          "default": "sqlite",
          "config": {
            "options": [
              "sqlite",
              "postgres"
            ]
          },
          "description": "Database engine to use."
        },
        {
          "name": "database_host",
          "type": "str",
          "title": "Database Host",
          "required": true,
          "needs": [
            "database_type=postgres"
          ],
          "default": "postgres",
          "config": {
            "placeholder": "postgres"
          },
          "description": "Hostname used for the external PostgreSQL database."
        },
        {
          "name": "database_port",
          "type": "int",
          "title": "Database Port",
          "required": true,
          "needs": [
            "database_type=postgres"
          ],
          "default": 5432,
          "config": {
            "placeholder": "5432"
          },
          "description": "Port used for the external PostgreSQL database."
        },
        {
          "name": "database_name",
          "type": "str",
          "title": "Database Name",
          "required": true,
          "needs": [
            "database_type=postgres"
          ],
          "default": "dockhand",
          "config": {
            "placeholder": "dockhand"
          },
          "description": "Database name used for PostgreSQL."
        },
        {
          "name": "database_user",
          "type": "str",
          "title": "Database User",
          "required": true,
          "needs": [
            "database_type=postgres"
          ],
          "default": "dockhand",
          "config": {
            "placeholder": "dockhand"
          },
          "description": "Username used for PostgreSQL."
        },
        {
          "name": "database_password",
          "type": "secret",
          "title": "Database Password",
          "required": true,
          "needs": [
            "database_type=postgres"
          ],
          "description": "Password used for PostgreSQL."
        }
      ]
    },
    {
      "title": "Authentication",
      "name": "authentication",
      "items": [
        {
          "name": "disable_local_login",
          "type": "bool",
          "title": "Disable Local Login",
          "required": false,
          "default": false,
          "description": "when SSO correctly setup, not recommended as first deployment"
        }
      ]
    }
  ]
}
