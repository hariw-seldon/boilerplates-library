{
  "slug": "adguardhome",
  "kind": "compose",
  "metadata": {
    "name": "AdGuard Home",
    "description": "Network-wide DNS-based ad and tracker blocker. AdGuard Home operates as a DNS server that blocks tracking domains, with support for DNS-over-HTTPS, DNS-over-TLS, DNS-over-QUIC, parental controls, and safe browsing filters.",
    "guide": "## Overview\nNetwork-wide DNS-based ad and tracker blocker. AdGuard Home operates as a DNS server that blocks tracking domains, with support for DNS-over-HTTPS, DNS-over-TLS, DNS-over-QUIC, parental controls, and safe browsing filters.\n\n## How to Use\nThis template renders `compose.yaml` as a file.\n\n1. Decide on service names, ports, volumes, credentials, and optional integrations before rendering the stack.\n2. Fill in the required variables first, then enable optional sections such as Traefik, metrics, or extra services only when needed.\n3. Render the Compose project, review the generated files, and start it with Docker Compose in the target directory.\n\n## Variable Guide\n### General\n- `service_name`: Name of the service; optional; default: adguardhome.\n- `restart_policy`: Policy used for Restart; required; default: unless-stopped; options: unless-stopped, always, on-failure, no.\n- `initial_setup`: Enable initial setup wizard on port 3000 (only used without Traefik); optional; default: true.\n\n### Network\n- `network_mode`: Mode used for Network; optional.\n- `network_name`: Name to use for the network; optional; default: adguardhome_network.\n- `network_macvlan_ipv4_address`: Network Macvlan Ipv4 Address; required; default: IP_ADDRESS; shown when: network_mode=macvlan.\n- `network_macvlan_parent_interface`: Network Macvlan Parent Interface; required; default: eth0; shown when: network_mode=macvlan.\n- `network_macvlan_subnet`: Network Macvlan Subnet; required; default: IP_RANGE; shown when: network_mode=macvlan.\n- `network_macvlan_gateway`: Network Macvlan Gateway; required; default: GATEWAY; shown when: network_mode=macvlan.\n- `network_external`: Whether the network is external; optional; default: false.\n\n### Ports\n- `ports_http`: Host port exposed for HTTP; optional; default: 80.\n- `ports_https`: Host port exposed for HTTPS; optional; default: 443.\n- `ports_dns`: Port used for DNS; required; default: 53.\n- `ports_initial`: Initial setup wizard port (only when Traefik is disabled); optional; default: 3000; shown when: traefik_enabled=false and initial_setup=true.\n- `ports_tls`: DNS over TLS Port; required; default: 853.\n- `ports_dnscrypt`: Port used for Dnscrypt; required; default: 5443.\n\n### Traefik\n- `traefik_enabled`: Enable Traefik integration; optional; default: false.\n- `traefik_host`: Hostname used for Traefik; optional.\n- `traefik_domain`: Domain used for Traefik; required.\n- `traefik_network`: Network used for Traefik; required; default: traefik.\n- `traefik_tls_enabled`: Enable Traefik TLS; optional; default: false.\n- `traefik_tls_certresolver`: Traefik Tls Certresolver; required; default: cloudflare; shown when: traefik_tls_enabled=true.\n\n### Volume\n- `volume_mode`: Mode used for Volume; required; default: local; options: local, mount, nfs.\n- `volume_mount_path`: Mount path used for the volume; required; default: /data; shown when: volume_mode=mount.\n- `volume_nfs_server`: Hostname or IP address of the NFS server; required; default: NFS_SERVER; shown when: volume_mode=nfs.\n- `volume_nfs_path`: Export path on the NFS server; required; default: /export; shown when: volume_mode=nfs.\n- `volume_nfs_options`: Mount options for the NFS volume; required; default: rw,nolock,soft; shown when: volume_mode=nfs.\n\n## References\n- [AdGuard Home](https://adguard.com/adguard-home/overview.html)\n- [Documentation](https://github.com/AdguardTeam/AdGuardHome/wiki)\n- [GitHub](https://github.com/AdguardTeam/AdGuardHome)\n## Notes\n- During initial setup, AdGuard Home runs an HTTP server on port 3000 for configuration. With Traefik enabled, access initial setup via `http://<container-ip>:3000`. Disable it (`initial_setup=false`) after setup is complete.\n- If you need DHCP or want AdGuard Home to bind directly to port 53, set `network_mode` to `host` or `macvlan`. This exposes all container ports on the host and is incompatible with `traefik_enabled`.",
    "tags": [
      "traefik",
      "network",
      "volume"
    ],
    "template_version": "v0.107.71",
    "icon": {
      "provider": "selfhst",
      "id": "adguard-home"
    },
    "draft": false
  },
  "variables": [
    {
      "title": "General",
      "name": "general",
      "items": [
        {
          "name": "service_name",
          "type": "str",
          "title": "Service Name",
          "required": false,
          "default": "adguardhome",
          "config": {
            "placeholder": "adguardhome"
          },
          "description": "Name of the service."
        },
        {
          "name": "restart_policy",
          "type": "enum",
          "title": "Restart Policy",
          "required": true,
          "default": "unless-stopped",
          "config": {
            "options": [
              "unless-stopped",
              "always",
              "on-failure",
              "no"
            ]
          },
          "description": "Policy used for Restart."
        },
        {
          "name": "initial_setup",
          "type": "bool",
          "title": "Initial Setup",
          "required": false,
          "default": true,
          "description": "Enable initial setup wizard on port 3000 (only used without Traefik)"
        }
      ]
    },
    {
      "title": "Network",
      "name": "network",
      "items": [
        {
          "name": "network_mode",
          "type": "str",
          "title": "Network Mode",
          "required": false,
          "description": "Mode used for Network."
        },
        {
          "name": "network_name",
          "type": "str",
          "title": "Network Name",
          "required": false,
          "default": "adguardhome_network",
          "config": {
            "placeholder": "adguardhome_network"
          },
          "description": "Name to use for the network."
        },
        {
          "name": "network_macvlan_ipv4_address",
          "type": "str",
          "title": "IPv4 Address",
          "required": true,
          "default": "IP_ADDRESS",
          "needs": [
            "network_mode=macvlan"
          ],
          "config": {
            "placeholder": "IP_ADDRESS"
          },
          "description": "Network Macvlan Ipv4 Address"
        },
        {
          "name": "network_macvlan_parent_interface",
          "type": "str",
          "title": "Parent Interface",
          "required": true,
          "default": "eth0",
          "needs": [
            "network_mode=macvlan"
          ],
          "config": {
            "placeholder": "eth0"
          },
          "description": "Network Macvlan Parent Interface"
        },
        {
          "name": "network_macvlan_subnet",
          "type": "str",
          "title": "Subnet",
          "required": true,
          "default": "IP_RANGE",
          "needs": [
            "network_mode=macvlan"
          ],
          "config": {
            "placeholder": "IP_RANGE"
          },
          "description": "Network Macvlan Subnet"
        },
        {
          "name": "network_macvlan_gateway",
          "type": "str",
          "title": "Gateway",
          "required": true,
          "default": "GATEWAY",
          "needs": [
            "network_mode=macvlan"
          ],
          "config": {
            "placeholder": "GATEWAY"
          },
          "description": "Network Macvlan Gateway"
        },
        {
          "name": "network_external",
          "type": "bool",
          "title": "External Network",
          "required": false,
          "default": false,
          "description": "Whether the network is external"
        }
      ]
    },
    {
      "title": "Ports",
      "name": "ports",
      "items": [
        {
          "name": "ports_http",
          "type": "str",
          "title": "HTTP Port",
          "required": false,
          "default": 80,
          "config": {
            "placeholder": "80"
          },
          "description": "Host port exposed for HTTP."
        },
        {
          "name": "ports_https",
          "type": "str",
          "title": "HTTPS Port",
          "required": false,
          "default": 443,
          "config": {
            "placeholder": "443"
          },
          "description": "Host port exposed for HTTPS."
        },
        {
          "name": "ports_dns",
          "type": "int",
          "title": "DNS Port",
          "required": true,
          "default": 53,
          "config": {
            "placeholder": "53"
          },
          "description": "Port used for DNS."
        },
        {
          "name": "ports_initial",
          "type": "int",
          "title": "Initial Setup Port",
          "required": false,
          "default": 3000,
          "description": "Initial setup wizard port (only when Traefik is disabled)",
          "needs": [
            "traefik_enabled=false",
            "initial_setup=true"
          ],
          "config": {
            "placeholder": "3000"
          }
        },
        {
          "name": "ports_tls",
          "type": "int",
          "title": "DoT Port",
          "required": true,
          "default": 853,
          "description": "DNS over TLS Port",
          "config": {
            "placeholder": "853"
          }
        },
        {
          "name": "ports_dnscrypt",
          "type": "int",
          "title": "DNSCrypt Port",
          "required": true,
          "default": 5443,
          "description": "Port used for Dnscrypt.",
          "config": {
            "placeholder": "5443"
          }
        }
      ]
    },
    {
      "title": "Traefik",
      "name": "traefik",
      "toggle": "traefik_enabled",
      "items": [
        {
          "name": "traefik_enabled",
          "type": "bool",
          "title": "Traefik",
          "required": false,
          "default": false,
          "description": "Enable Traefik integration"
        },
        {
          "name": "traefik_host",
          "type": "str",
          "title": "Host",
          "required": false,
          "config": {
            "placeholder": "adguardhome"
          },
          "description": "Hostname used for Traefik."
        },
        {
          "name": "traefik_domain",
          "type": "str",
          "title": "Domain",
          "required": true,
          "config": {
            "placeholder": "home.arpa"
          },
          "description": "Domain used for Traefik."
        },
        {
          "name": "traefik_entrypoint",
          "type": "str",
          "title": "Entrypoint",
          "required": true,
          "default": "web",
          "description": "Entrypoint name.",
          "config": {
            "placeholder": "web"
          }
        },
        {
          "name": "traefik_network",
          "type": "str",
          "title": "Network",
          "required": true,
          "default": "traefik",
          "config": {
            "placeholder": "traefik"
          },
          "description": "Network used for Traefik."
        },
        {
          "name": "traefik_tls_enabled",
          "type": "bool",
          "title": "TLS",
          "required": false,
          "default": false,
          "description": "Enable Traefik TLS"
        },
        {
          "name": "traefik_tls_entrypoint",
          "type": "str",
          "title": "TLS Entrypoint",
          "required": true,
          "default": "websecure",
          "description": "Entrypoint name.",
          "config": {
            "placeholder": "websecure"
          },
          "needs": [
            "traefik_tls_enabled=true"
          ]
        },
        {
          "name": "traefik_tls_certresolver",
          "type": "str",
          "title": "Cert Resolver",
          "required": true,
          "default": "cloudflare",
          "config": {
            "placeholder": "cloudflare"
          },
          "description": "Traefik Tls Certresolver",
          "needs": [
            "traefik_tls_enabled=true"
          ]
        }
      ]
    },
    {
      "title": "Volume",
      "name": "volume",
      "items": [
        {
          "name": "volume_mode",
          "type": "enum",
          "title": "Volume Mode",
          "required": true,
          "default": "local",
          "config": {
            "options": [
              "local",
              "mount",
              "nfs"
            ]
          },
          "description": "Mode used for Volume."
        },
        {
          "name": "volume_mount_path",
          "type": "str",
          "title": "Volume Mount Path",
          "required": true,
          "default": "/data",
          "needs": [
            "volume_mode=mount"
          ],
          "config": {
            "placeholder": "/data"
          },
          "description": "Mount path used for the volume."
        },
        {
          "name": "volume_nfs_server",
          "type": "str",
          "title": "Volume Nfs Server",
          "required": true,
          "default": "NFS_SERVER",
          "needs": [
            "volume_mode=nfs"
          ],
          "config": {
            "placeholder": "NFS_SERVER"
          },
          "description": "Hostname or IP address of the NFS server."
        },
        {
          "name": "volume_nfs_path",
          "type": "str",
          "title": "Volume Nfs Path",
          "required": true,
          "default": "/export",
          "needs": [
            "volume_mode=nfs"
          ],
          "config": {
            "placeholder": "/export"
          },
          "description": "Export path on the NFS server."
        },
        {
          "name": "volume_nfs_options",
          "type": "str",
          "title": "Volume Nfs Options",
          "required": true,
          "default": "rw,nolock,soft",
          "needs": [
            "volume_mode=nfs"
          ],
          "config": {
            "placeholder": "rw,nolock,soft"
          },
          "description": "Mount options for the NFS volume."
        }
      ]
    }
  ]
}
