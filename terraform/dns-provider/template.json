{
  "slug": "dns-provider",
  "kind": "terraform",
  "metadata": {
    "name": "DNS Provider",
    "description": "Configures the HashiCorp DNS Terraform provider with TSIG update credentials.",
    "tags": [
      "dns",
      "terraform",
      "provider"
    ],
    "icon": {
      "provider": "selfhst",
      "id": "bind-9",
      "color": "default"
    },
    "draft": false,
    "version": {
      "name": "3.5.0",
      "source_dep_name": "hashicorp/dns",
      "source_dep_version": "3.5.0"
    }
  },
  "variables": [
    {
      "title": "Provider Configuration",
      "name": "provider",
      "items": [
        {
          "name": "dns_server",
          "type": "str",
          "title": "DNS Server",
          "required": true,
          "description": "DNS server that accepts TSIG-authenticated updates.",
          "config": {
            "placeholder": "192.168.178.10"
          }
        },
        {
          "name": "dns_server_tsig_key_secret",
          "type": "secret",
          "title": "TSIG Key Secret",
          "required": true,
          "description": "TSIG key secret for dynamic DNS updates."
        }
      ]
    }
  ]
}
