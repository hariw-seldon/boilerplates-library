{
  "slug": "docker-postgres-restore",
  "kind": "bash",
  "metadata": {
    "name": "Docker Postgres Restore",
    "description": "One-shot Bash restore script for importing a .sql or .sql.gz PostgreSQL dump into a running Docker container with preflight checks, confirmation flow, optional drop/recreate, and dry-run support.",
    "tags": [
      "docker",
      "restore",
      "postgres",
      "database",
      "bash"
    ],
    "template_version": "1.0.0",
    "icon": {
      "provider": "simple-icons",
      "id": "postgresql",
      "color": "emerald"
    },
    "draft": false
  },
  "variables": [
    {
      "title": "Restore",
      "name": "restore",
      "items": [
        {
          "name": "container_name",
          "type": "str",
          "title": "Container Name",
          "required": true,
          "default": "postgres",
          "description": "Name of the running PostgreSQL container.",
          "config": {
            "placeholder": "postgres"
          }
        },
        {
          "name": "archive_path",
          "type": "str",
          "title": "Archive Path",
          "required": true,
          "default": "/backups/postgres/backup_postgres_postgres_latest.sql.gz",
          "description": "Absolute path to the .sql or .sql.gz dump archive on the Docker host.",
          "config": {
            "placeholder": "/backups/postgres/backup_postgres_postgres_latest.sql.gz"
          }
        },
        {
          "name": "postgres_user",
          "type": "str",
          "title": "Postgres User",
          "required": false,
          "default": "postgres",
          "description": "Database user used by psql for the restore.",
          "config": {
            "placeholder": "postgres"
          }
        },
        {
          "name": "postgres_database",
          "type": "str",
          "title": "Database Name",
          "required": false,
          "default": "all",
          "description": "Target database to restore, or 'all' for an all-databases dump.",
          "config": {
            "placeholder": "all"
          }
        },
        {
          "name": "postgres_password_env",
          "type": "str",
          "title": "Password Env Variable",
          "required": false,
          "default": "PGPASSWORD",
          "description": "Host environment variable that stores the PostgreSQL password.",
          "config": {
            "placeholder": "PGPASSWORD"
          }
        }
      ]
    },
    {
      "title": "Safety",
      "name": "safety",
      "items": [
        {
          "name": "drop_recreate_database",
          "type": "bool",
          "title": "Drop/Recreate Database",
          "required": false,
          "default": false,
          "description": "If enabled, terminate connections and recreate the target database before restore."
        },
        {
          "name": "dry_run",
          "type": "bool",
          "title": "Dry Run",
          "required": false,
          "default": false,
          "description": "Print planned actions without changing the container."
        },
        {
          "name": "auto_yes",
          "type": "bool",
          "title": "Auto Approve",
          "required": false,
          "default": false,
          "description": "Skip the interactive confirmation prompt."
        }
      ]
    }
  ]
}
